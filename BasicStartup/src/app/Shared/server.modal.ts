export class ServerEventData {
    constructor (public serverName: string, public serverDesc: string) { }
}

export class Server {
    constructor (public name: string, public content: string, public type: string) { }
}

